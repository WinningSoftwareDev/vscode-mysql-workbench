# Changelog

## [0.1.0] - Unreleased

Initial scaffold.

- Connection management (add / edit / delete) with passwords in SecretStorage.
- Whole-server tree: connection → schema → table → column, lazy-loaded.
- SQL console with a results grid; `Ctrl`/`Cmd`+`Enter` to run.
- Table row preview from the tree.
- Run the active `.sql` file / selection against a chosen connection.
